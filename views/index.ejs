<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-10">
        <!-- Container Box -->
        <div class="bg-zinc-800 border border-zinc-700 rounded-xl p-10 w-full max-w-md shadow-lg">
            <h3 class="text-3xl mb-6 text-center font-semibold">CREATE ACCOUNT</h3>
            <form action="/register" method="POST" class="flex flex-col gap-4">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="text" placeholder="Name" name="name">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="text" placeholder="Username" name="username">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="password" placeholder="Password" name="password">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="email" placeholder="Email" name="email">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="number" placeholder="Age" name="age">
                <input class="px-5 py-3 rounded-md bg-blue-500 hover:bg-blue-600 text-white cursor-pointer" type="submit" value="Create Account">
            </form>

            <!-- Login Section -->
            <div class="mt-6 text-center">
                <p class="mb-2 text-zinc-400">Already have an account?</p>
                <a href="/login" class="inline-block px-5 py-3 rounded-md bg-green-500 hover:bg-green-600 text-white text-sm">Login</a>
            </div>
        </div>
    </div>
</body>
</html>
