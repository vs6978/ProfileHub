<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex items-center justify-center p-10">
        <!-- Container Box -->
        <div class="bg-zinc-800 border border-zinc-700 rounded-xl p-10 w-full max-w-md shadow-lg">
            <h3 class="text-3xl mb-6 text-center font-semibold">LOGIN YOUR ACCOUNT</h3>
            
            <form action="/login" method="POST" class="flex flex-col gap-4">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="email" placeholder="Email" name="email">
                <input class="px-3 py-3 rounded-md outline-none bg-transparent border-2 border-zinc-700" type="password" placeholder="Password" name="password">
                <input class="px-5 py-3 rounded-md bg-blue-500 hover:bg-blue-600 text-white cursor-pointer" type="submit" value="Login">
            </form>

            <!-- Account creation prompt -->
            <div class="text-center mt-6">
                <p class="mb-3 text-sm text-zinc-400">Don't have an account?</p>
                <a href="http://localhost:3000" class="inline-block bg-green-500 hover:bg-green-600 px-5 py-2 rounded-md text-white text-sm">Create Account</a>
            </div>
        </div>
    </div>
</body>
</html>
